# PostgreSQL Client Authentication Configuration File (Production)
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             postgres                                peer
local   all             all                                     scram-sha-256

# IPv4 local connections:
host    all             postgres        127.0.0.1/32            scram-sha-256
host    all             postgres        ::1/128                 scram-sha-256

# Production database connections
host    gasolinera_jsm  gasolinera_user 172.22.0.0/16          scram-sha-256
host    gasolinera_jsm  gasolinera_user 10.0.0.0/8             scram-sha-256

# Replication connections
host    replication     replica_user    172.22.0.0/16          scram-sha-256
host    replication     replica_user    10.0.0.0/8             scram-sha-256

# Application service connections (Docker network)
host    gasolinera_jsm  gasolinera_user 172.22.0.0/16          scram-sha-256

# Monitoring connections
host    gasolinera_jsm  monitoring_user 172.22.0.0/16          scram-sha-256

# Backup connections
host    gasolinera_jsm  backup_user     172.22.0.0/16          scram-sha-256

# SSL connections from trusted networks
hostssl gasolinera_jsm  gasolinera_user 0.0.0.0/0              scram-sha-256 clientcert=1
hostssl replication     replica_user    0.0.0.0/0              scram-sha-256 clientcert=1

# Deny all other connections
host    all             all             0.0.0.0/0              reject
host    all             all             ::/0                   reject