package com.gasolinerajsm.adengine.model

enum class AdType {
    BANNER, VIDEO, INTERSTITIAL, NATIVE
}

enum class AdStatus {
    DRAFT, PENDING_APPROVAL, ACTIVE, PAUSED, COMPLETED, CANCELLED, REJECTED
}