{
  "name": "owner-dashboard",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/owner-dashboard/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "command": "next build",
        "outputPath": "dist/apps/owner-dashboard"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/owner-dashboard",
          "command": "next build"
        },
        "production": {
          "command": "next build"
        }
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "owner-dashboard:build",
        "dev": true,
        "port": 3010
      },
      "configurations": {
        "development": {
          "buildTarget": "owner-dashboard:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "owner-dashboard:build:production",
          "dev": false
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/owner-dashboard/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
